# 🚀 MergeMeet 快速開始指南

## Week 0.5 環境設置 - 已完成！✅

### 已建立的內容

#### 📁 專案結構
```
mergemeet/
├── backend/              ✅ 後端 FastAPI 應用
│   ├── app/
│   │   ├── api/         ✅ API 路由 (待實作)
│   │   ├── core/        ✅ 核心配置 (config.py, database.py)
│   │   ├── models/      ✅ 資料庫模型 (待實作)
│   │   ├── schemas/     ✅ Pydantic Schemas (待實作)
│   │   ├── services/    ✅ 業務邏輯 (待實作)
│   │   └── main.py      ✅ 主應用 (含 Hello World API)
│   ├── tests/           ✅ 測試框架 (pytest + conftest)
│   ├── uploads/         ✅ 檔案上傳目錄
│   ├── requirements.txt ✅ Python 依賴
│   └── pytest.ini       ✅ 測試配置
│
├── frontend/            ✅ 前端 Vue 3 應用
│   ├── src/
│   │   ├── components/  ✅ Vue 組件 (待實作)
│   │   ├── views/       ✅ 頁面 (Home.vue)
│   │   ├── stores/      ✅ Pinia Stores (待實作)
│   │   ├── router/      ✅ Vue Router
│   │   └── main.js      ✅ 入口文件
│   ├── package.json     ✅ Node.js 依賴
│   └── vite.config.js   ✅ Vite 配置
│
├── scripts/             ✅ 工具腳本
│   ├── setup.sh        ✅ 環境設置
│   ├── dev.sh          ✅ 啟動開發
│   ├── test.sh         ✅ 執行測試
│   └── check.sh        ✅ 狀態檢查
│
├── docker-compose.yml   ✅ Docker 配置 (PostgreSQL + Redis)
├── .gitignore          ✅ Git 忽略檔案
└── README.md           ✅ 專案說明
```

#### ✨ 已完成的功能

**後端 (FastAPI):**
- ✅ FastAPI 應用結構
- ✅ 配置管理 (Settings)
- ✅ 資料庫連接 (SQLAlchemy + Async)
- ✅ CORS 中間件
- ✅ Hello World API (`/api/hello`)
- ✅ 健康檢查 API (`/health`)
- ✅ pytest 測試框架
- ✅ 測試覆蓋率配置

**前端 (Vue 3):**
- ✅ Vue 3 + Vite 配置
- ✅ Vue Router 設置
- ✅ Pinia 狀態管理準備
- ✅ Axios API 客戶端
- ✅ 首頁 (Home.vue) 含後端連接測試
- ✅ 響應式設計

**DevOps:**
- ✅ Docker Compose (PostgreSQL + PostGIS + Redis)
- ✅ 一鍵設置腳本
- ✅ 一鍵啟動腳本
- ✅ 測試執行腳本
- ✅ 狀態檢查腳本

### 📊 檔案統計

- Python 檔案: **12 個**
- Vue/JS 檔案: **5 個**
- 配置檔案: **4 個**
- 工具腳本: **4 個**
- **總計: 30 個檔案**

---

## 🎯 下一步：Week 1 - 認證系統

### 任務清單

#### 後端任務
1. **資料庫 Schema (TDD)**
   - [ ] User 模型
   - [ ] Email 驗證表
   - [ ] Session 管理

2. **JWT 認證**
   - [ ] Token 生成與驗證
   - [ ] Refresh Token 機制

3. **API 端點**
   - [ ] POST `/api/auth/register` - 註冊
   - [ ] POST `/api/auth/login` - 登入
   - [ ] POST `/api/auth/verify-email` - Email 驗證
   - [ ] POST `/api/auth/refresh` - 刷新 Token
   - [ ] POST `/api/auth/logout` - 登出

#### 前端任務
1. **頁面開發**
   - [ ] 註冊頁面
   - [ ] 登入頁面
   - [ ] Email 驗證頁面

2. **狀態管理**
   - [ ] 用戶 Store (Pinia)
   - [ ] 認證狀態管理

3. **API 整合**
   - [ ] API 客戶端封裝
   - [ ] 請求攔截器 (Token 注入)
   - [ ] 錯誤處理

### 驗收標準
- [ ] 可以註冊新用戶
- [ ] 可以登入並取得 JWT Token
- [ ] 可以驗證 Email
- [ ] 可以重設密碼
- [ ] 測試覆蓋率 > 80%

---

## 🚀 本機測試指令

```bash
# 1. 啟動資料庫 (需要 Docker)
docker compose up -d postgres redis

# 2. 啟動後端
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload

# 3. 啟動前端 (另一個終端)
cd frontend
npm install
npm run dev

# 4. 執行測試
cd backend
pytest -v

# 5. 訪問應用
# 前端: http://localhost:5173
# 後端 API: http://localhost:8000
# API 文檔: http://localhost:8000/docs
```

---

## 📝 Git 提交訊息範例

```bash
git add .
git commit -m "feat: Week 0.5 - 完成開發環境設置

- 建立 FastAPI 後端結構
- 建立 Vue 3 前端結構
- 配置 Docker Compose (PostgreSQL + Redis)
- 建立測試框架 (pytest)
- 建立工具腳本 (setup, dev, test, check)
- 實作 Hello World API
- 實作前端首頁與後端連接測試

驗收: ✅ 專案結構完整，可執行基本功能"
```

---

## ✅ Week 0.5 完成檢查清單

- ✅ 後端可執行 Hello World API
- ✅ 前端結構完整
- ✅ 測試框架配置完成
- ✅ Docker Compose 配置完成
- ✅ 工具腳本建立完成
- ✅ 文檔撰寫完成
- ✅ Git 版本控制設置

**狀態**: 🎉 Week 0.5 已完成！可以開始 Week 1 開發。

