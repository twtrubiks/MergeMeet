# MergeMeet Frontend 環境變數範例
#
# 預設採同源相對路徑：dev 由 Vite proxy 轉發（vite.config.js），
# prod 由反向代理轉發（/api、/ws、/uploads 轉給後端），無需設定任何變數。
# 以下變數僅在前後端「拆網域」部署時才需要覆寫，平常保持註解即可。
# 所有設定統一由 src/config/index.js 讀取，禁止在其他檔案直接讀 import.meta.env。

# API 基礎路徑（預設 /api）
# VITE_API_BASE_URL=https://api.example.com/api

# WebSocket 完整網址（預設 ws(s)://<目前網域>/ws）
# VITE_WS_URL=wss://api.example.com/ws

# 媒體檔案基礎網址（預設空字串，直接使用後端回傳的相對路徑）
# VITE_MEDIA_BASE_URL=https://api.example.com

# 文件監視模式
# Docker/WSL/VM 環境建議使用 polling 模式
# 一般環境可設為 false 以節省 CPU
# VITE_USE_POLLING=false
