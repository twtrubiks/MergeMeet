[flake8]
max-line-length = 100
# 忽略某些規則（可選）
# extend-ignore = E203, W503

# 排除特定目錄
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    alembic/versions,
    .pytest_cache

# 每個檔案最大複雜度
max-complexity = 10
