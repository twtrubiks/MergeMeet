# Docker Compose 環境變數
# 複製此檔案為 .env 即可覆寫（未設定時 docker-compose.yml 使用開發預設值）
# ⚠️ 正式環境請務必設定為安全密碼（openssl rand -hex 16 生成）
#
# 注意：更換密碼後，backend/.env 的 DATABASE_URL 與 REDIS_URL 也需同步更新

POSTGRES_PASSWORD=mergemeet123
REDIS_PASSWORD=mergemeet_redis123
